# P1 인수인계 (2026-08-28)

다음 세션 시작 시 이 파일과 `docs/plan/still-young-days-p1-ui-mock-plan-v0.1.md` §7을 먼저 읽는다.

## 저장소 2개 (기획 문서는 2026-08-28 앱 저장소로 합침; `~/myworkspace/still-young`은 폐기 가능)
| 용도 | 경로 | git 상태 |
|---|---|---|
| Flutter 앱 + 기획·문서(`docs/`, `ideas/`) | `~/myworkspace/still-young-days-app` | `main` → `https://github.com/JKPark83/still-young-days` (push는 `gh auth switch -u JKPark83` 후) |
| 데이터 파이프라인 | `~/myworkspace/still-young-days-data` | 로컬 커밋만. GitHub private 저장소 생성은 사용자 몫 |

## 환경
- Flutter 3.47.2 / Dart 3.13.2 → `~/development/flutter` (`~/.zshrc`에 PATH 추가됨. 비대화형 셸에서는 `export PATH="$HOME/development/flutter/bin:$PATH"` 필요)
- Android SDK `~/Library/Android/sdk` (platform 36, build-tools 36.0.0, 라이선스 수락됨)
- **에뮬레이터 사용 불가**: API 29/34 arm64 AVD 모두 부팅은 되나 아무 앱이나 실행하면 VM 전체가 멈춤(adb 무응답). 앱 문제 아님. 실기기로 확인할 것.

## P1 현재 상태
- 7화면 전부 구현 (splash, location_intro, home, detail, settings+text_size, region_picker, howto)
- `flutter analyze` 0건, `flutter test` **38/38 통과**
- 디버그·릴리즈 APK 빌드 성공 (`build/app/outputs/flutter-apk/`)
- 문서: 앱 `README.md`(실행법·구조·가정), `docs/wireframes/CHECKLIST.md`(13×7, 구현 열 완료), 계획서 §7 갱신

## 핵심 설계 결정 (되돌리지 말 것)
- 상태관리 패키지 없음: `FutureBuilder` + `setState` + `ValueNotifier`, Navigator 1.0
- 의존성: `url_launcher`, `shared_preferences`만
- 글자 배율 = OS × 앱(1.0/1.3/1.6), `withClampedTextScaling(1.0~2.0)`. 높이는 전부 dp 상수(`lib/theme/tokens.dart`)
- 토스트 금지 → `PersistentNotice` 배너. 비활성 버튼 금지 → 첫/마지막 카드에서도 버튼 활성 + 안내 문구
- 전화: `Uri(scheme:'tel')` + `launchUrl`, 실패 시 번호를 배너에 표시. `debugPrint('metric:call_tap')`은 P3 지표 자리
- `ItemRepository` 인터페이스 — P2에서 `MockItemRepository`만 교체. 목 JSON 스키마(schemaVersion 1) = P2 계약
- `regions.json`: 시/도 16개(전남·광주 통합, `12xxx` 코드는 가정값), 경기도 31개 시군구만 실제 코드
- 테스트 인프라(`test/helpers.dart`): 360×780 뷰, `FakePhone`, `SharedPreferences.setMockInitialValues`, 테스트마다 `rootBundle.clear()` (안 하면 두 번째 테스트부터 pumpAndSettle 타임아웃)
- `RegionName` 위젯: `nameOf()` Future를 캐시. FutureBuilder에 매 빌드 새 Future 넘기면 무한 리빌드됨

## P2 진행 상태 (2026-08-28 저녁)
- **M5(앱) 완료**: `RemoteItemRepository`(ETag, utf8 bodyBytes, 8s 타임아웃) + `FeedCache`(파일) + 캐시 폴백(`RegionFeed.fromCache`), 스플래시 "다시 시도", 홈 "새 정보를 못 받았어요"/"정보가 오래됐어요(48h)" 배너. `AppDeps.clock` 주입. 테스트 53/53.
  - 기본은 여전히 목 데이터. `--dart-define=FEED_BASE_URL=https://<host>/data/`를 주면 원격. `FEED_READ_TOKEN`은 private raw용(권장 안 함).
- **데이터 저장소 `still-young-days-data` (로컬 커밋 2건, 64 테스트)**: senuri_client(미호출)·regions·normalize·collect·guard(시도 전체 0건 → 커밋 안 함)·probe, `test.yml`/`collect.yml`, `.env.example`.
  - 시군구 표는 **실제 2026-07-01 자료**(admdongkor `sgg_20260701`, 256곳) → `tables/sigungu.json`. 행안부 원본은 로그인 필요라 미확보.
  - **오픈 이슈 #4 해소, 계획서 오기**: 광주 동구 = `12210`, `12110`은 목포시. 29/46 코드 없음(테스트로 고정). 앱 `regions.json`도 이 표로 재생성(16 시도·230 항목).
  - 모시 13곳은 구 파일 + **모시 코드 합본 파일**(`jobs/41110.json`)을 같이 낸다 — 앱은 모시 코드만 고르므로 합본을 읽는다.
  - 오픈 이슈 #2(private 데이터 접근)는 아직 미결. 앱은 `FEED_BASE_URL`만 있으면 되므로 `data/`를 public Pages로 내는 안을 권장.
- M1(실측)·M3 첫 수집·M4 cron 실증은 **인증키가 있어야** 한다.

## 남은 일
1. [사용자] data.go.kr 15015153 활용신청 → 인증키 → `still-young-days-data/.env`의 `DATA_GO_KR_KEY` + GitHub secret
2. [사용자] `gh repo create still-young-days-data --private` 후 push. 오픈 이슈 #2 결정(권장: `data/`만 public Pages)
3. [사용자] Claude Design 와이어프레임 7장 (`docs/plan/claude-design-prompt.md`) → `docs/wireframes/*.png` + CHECKLIST W열
4. [실기기] 스플래시→홈→상세→다이얼러(`031-000-0001`) 완주, 수원시로 바꿔 빈 상태 확인, TalkBack, OS 최대 글자 크기 — 이 Mac에 기기 연결 안 됨
5. 키가 오면 M1 probe → M3 `python -m pipeline.collect` → M4 workflow_dispatch → 앱을 `FEED_BASE_URL`로 실행
